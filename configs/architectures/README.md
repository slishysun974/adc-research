# 架构配置

`gu_behavioral_v1.yaml` 是 Gate A/B/C 通过后的静态行为冻结基线。它能追溯到 `docs/specifications/GU_ARCHITECTURE_EVIDENCE_V1.md` 与 `GU_SIGNAL_CODE_CONVENTIONS_V1.md`，但不声称复原硅内部 bit-true 真值表。

`gu_nominal_v0_1_draft.yaml` 与 `gu_static_hypothesis_a_v0_1.yaml` 保留为证据形成历史和旧实验复现输入。
